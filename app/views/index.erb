<!<!DOCTYPE html>
<html>
    <head>
    <title>Finstagram</title>
    <!-- .id generates unique id -->
  <link rel="stylesheet" href="../../public/stylesheets/normalize.css"/>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto+Condensed:300italic,700italic,700,300"/>
  <link rel="stylesheet" href="../../public/stylesheets/lib.css"/>
  <link rel="stylesheet" href="../../public/stylesheets/app.css" />
  
    </head>
    <body>
     <header> 
        <h1> Finstagram </h1>
        <div class ="login-info">
            <% if current_user %>
            <%= current_user.username %>
            <a href="/logout">Logout</a>
            <% else %>
             <a href="/login">Login</a>
             <a href="/signup">Signup</a>
             <% end %>
        </div>
     </header>
     
     
        <main role="main"><!-- not understood -->
           <% @posts.each do |post| %>
             <article class="post">
             <div class="user-info"> 
        
                     <img src="<%= post.user.avatar_url %>" alt="<%= post.user.username %>">
                 <h2><%= post.user.username %></h2>
                 <h3><%= post.humanized_time_ago %></h3>
             </div>
              <a class="photo" href="#">
                  <img src= "<%= post.photo_url %>" alt= "<%= post.user.username %>">
                </a>
                
                <!-- id gets # in css, class gets period -->
             
            <div class="actions">
                <%= post.like_count %> likes
            <span class="comment-count"><%= post.comment_count %> comments</span>
            </div>
            <ul class="comments">
              <% post.comments.each do |comment| %>
                <li>
                    <p>
                        <%= comment.user.username %>: <%= comment.text %>
                    </p>
                </li>
              
            </ul>
            
             </article>
            <% end %>
  
<% end %>
</main>
    </body>
</html>